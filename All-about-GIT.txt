touch .gitignore

git config --global user.name "Md Akram Khan"
git config --global user.email "akramkhan.dev@gmail.com"
git init

git add .

git commit -m "Initial commit"

git status
{
	On branch master
	nothing to commit, working tree clean
}

git remote add origin git@github.com:developer-akram/javaScript_Programs.git

git push origin master

ssh-keygen -t rsa -b 4096 -C "akramkhan.dev@gmail.com"

cat /c/Users/Administrator/.ssh/id_rsa.pub
		or
tail /c/Users/Administrator/.ssh/id_rsa.pub

git push origin master

git remote add origin git@github.com:developer-akram/javaScript_Programs.git

git push origin master

//Add files then update it

git status

you will get new files

git add .

git commit -m "Added Filename"

git push origin master
